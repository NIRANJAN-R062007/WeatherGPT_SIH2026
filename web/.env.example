# localhost:8001 is the local-dev default (the orchestrator run from
# services/orchestrator/). Point it at https://3-108-52-61.sslip.io to exercise
# the live backend — the one the deployed prototype/frontend talks to. web/ is
# not deployed anywhere; in production this would be injected at build time by
# the host — never commit a real production value here.
VITE_API_BASE=http://localhost:8001
